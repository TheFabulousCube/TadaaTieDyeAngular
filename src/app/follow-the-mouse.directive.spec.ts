import { FollowTheMouseDirective } from './follow-the-mouse.directive';

describe('FollowTheMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowTheMouseDirective();
    expect(directive).toBeTruthy();
  });
});
